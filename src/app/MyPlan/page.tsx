import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: "FITLOG-MyPlan",
  description: "...",
};

const myPlan = () => {

    
    return (
        <div>
            <h2>My plans</h2>
        </div>
    );
};

export default myPlan;